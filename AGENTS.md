IMPORTANT: You MUST check available skills before responding to ANY user request. 
If a skill exists that can help with the task, ALWAYS use it first.

**We have three important mcps:**
- codebase memory which creates a graph of the codebase so you can easily find and search for classes, methods and anything else in the code
- context7 which can be used to read and view documentation based on the current version of packages
- sequential which is used for complex thinking and problem solving.